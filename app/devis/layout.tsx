import { ReactNode } from 'react';
import { metadata } from './metadata';

export { metadata };

export default function DevisLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}