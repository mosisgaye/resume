import { ReactNode } from 'react';
import { metadata } from './metadata';

export { metadata };

export default function ServicesFranceLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}